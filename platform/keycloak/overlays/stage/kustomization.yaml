apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  # SealedSecrets for the keycloak namespace (sealed against stage cluster).
  # Authored by the user via kubeseal — see ../../README.md.
  - secrets

# Stage overlay — base hostname is already keycloak-stage.brianbeck.net
# and instance count is 1, so no patches needed today. Reserved for future
# stage-specific tweaks.
