# CloudNativePG prod overlay — HA operator with PDB.

replicaCount: 2

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 1000m
    memory: 512Mi

podDisruptionBudget:
  enabled: true
  minAvailable: 1
